# Kass
### A [processing](https://processing.org/) clone writed on python using pygame

## Why?
The main goal is to get a module that uses a simpler syntax than pygame but with almost every tool implemented in processing.